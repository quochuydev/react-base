import React from 'react';

export default function Line(props: TODO): TODO {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        marginTop: 10,
      }}
    >
      {props.children}
    </div>
  );
}
